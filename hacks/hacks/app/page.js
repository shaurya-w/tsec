import Scene from  "../components/Scene";
import UnifiedDashboard from "./pages/main";
import CooperApp from "./pages/main";

export default function Home() {
  return (
    <main className="relative w-full h-screen">
  
      <UnifiedDashboard/>
      {/* 2. Content Overlay */}
    </main>
  );
}